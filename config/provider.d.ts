import { ReactNode } from 'react';
export interface ConfigProviderProps {
    children: ReactNode;
}
declare const ConfigProvider: ({ children }: ConfigProviderProps) => import("react/jsx-runtime").JSX.Element;
export default ConfigProvider;
